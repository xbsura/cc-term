# Make the outer tmate session visually quiet so the inner tmux session
# feels closer to the normal local workflow.
set -g status off
set -g prefix C-]
bind-key ] send-prefix
set -g mouse on
set -g default-terminal "screen-256color"
